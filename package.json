{
  "name": "513-auto-clean-site",
  "private": true,
  "type": "module",
  "description": "Static site plus Netlify functions. The app source lives in web/.",
  "scripts": {
    "build": "cd web && npm ci && npm run build:funnel && npm run build:server && npm run build:site && npm run build:terms",
    "test": "cd web && npm test"
  },
  "dependencies": {
    "stripe": "^20.0.0"
  }
}
